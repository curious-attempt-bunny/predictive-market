<h1> New Event </h1>

<p>An event and it's outcomes need to be unambiguous. When the event is resolved will everyone agree which outcome won?</p>
  
<%= form_for @event, role: 'form' do |f| %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: 'form-control' %>
  </div>

  <% 5.times do |i| %>

    <div class="form-group">
      <%= f.label :name, "Outcome #{i + 1}" %>
      <%= f.text_field :name, name: 'event[outcomes_attributes][][name]', class: 'form-control', placeholder: 'Fill in as many outcomes as needed' %>
      <%= f.hidden_field :id, name: 'event[outcomes_attributes][][event_id]', class: 'form-control' %>
    </div>

  <% end %>

  <%= f.submit "Create" %>
<% end %>
